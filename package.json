{
  "name": "triad-ai",
  "version": "1.0.0",
  "engines": {
    "node": "22.13.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run dev && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "node": "23.13.1"
  }
}
